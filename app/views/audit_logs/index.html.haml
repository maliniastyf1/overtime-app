%h1 Audit Logs Dashboard

%table.table.table-striped.table-hover
  %thead
    %tr
      %th
        \#
      %th
        Emploee
      %th
        Week Starting
      %th
        Confirmed at
      %th
        Status
      %th

  %tbody#audit_logs
    = render @audit_logs

#paginator
  = paginate @audit_logs, remote: true
